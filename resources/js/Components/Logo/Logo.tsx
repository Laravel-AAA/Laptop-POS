export default function Logo({ className }: { className: string }) {
  return (
    <img
      className={className}
      src="/assets/logo/laptop-pos-logo.svg"
      alt="Application Logo"
    />
  );
}
